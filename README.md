# SUIAPI
Artworks & Disney Characters API Client

📖 Описание
Swift Package Manager модуль для работы с API художественных произведений и персонажей Disney. Модуль предоставляет удобный клиент для доступа к данным о произведениях искусства и персонажах из вселенной Disney.

🚀 Возможности
🎨 Artworks API - Получение информации о художественных произведениях

🏰 Disney Characters API - Работа с данными о персонажах Disney

📡 Сетевое взаимодействие - Готовые реализации для URLSession

🔮 Codable поддержка - Полная совместимость с Swift Codable

🎯 Типобезопасность - Статическая типизация всех моделей данных

📦 Установка
Swift Package Manager
Добавьте зависимость в ваш Package.swift:

swift
dependencies: [
    .package(url: "https://github.com/DmitriyNikonorov/SUIAPI")
]
Или через Xcode:

File → Add Packages...

Вставьте URL репозитория

Выберите версию

Добавьте к вашему target

🛠 Использование
Работа с художественными произведениями

```swift
import SUIAPI

// Получение списка произведений искусства
do {
    let response = try await ArtworksAPI.getArtworks(page: 1, limit: 20)
    print("Найдено произведений: \(response.data.count)")
    for artwork in response.data {
        print("Произведение: \(artwork.title ?? "Без названия")")
    }
} catch {
    print("Ошибка: \(error)")
}

// Получение конкретного произведения по ID
do {
    let artwork = try await ArtworksAPI.getArtworkById(id: 12345)
    print("Детали произведения: \(artwork.data.title ?? "Без названия")")
} catch {
    print("Ошибка: \(error)")
}
```

Работа с персонажами Disney

```swift
import SUIAPI

// Получение списка персонажей Disney
do {
    let response = try await DisneyCharactersAPI.getDisneyCharacters(page: 1, pageSize: 30)
    print("Найдено персонажей: \(response.data.count)")
    for character in response.data {
        print("Персонаж: \(character.name) - \(character.films?.first ?? "Нет фильмов")")
    }
} catch {
    print("Ошибка: \(error)")
}

// Получение конкретного персонажа по ID
do {
    let character = try await DisneyCharactersAPI.getDisneyCharacterById(id: 100)
    print("Детали персонажа: \(character.name)")
    print("Фильмы: \(character.films?.joined(separator: ", ") ?? "Нет фильмов")")
} catch {
    print("Ошибка: \(error)")
}
```


🏗 Архитектура
Основные компоненты
```text
API/
├── APIs/
│   ├── ArtworksAPI.swift          // API для художественных произведений
│   └── DisneyCharactersAPI.swift  // API для персонажей Disney
├── Models/
|   {...} ──                       // множество вспомогательных файлов
│   ├── Artwork.swift              // Модель художественного произведения
│   ├── ArtworkResponse.swift      // Ответ с произведением
│   ├── DisneyCharacter.swift      // Модель персонажа Disney
│   └── DisneyCharactersResponse.swift // Ответ с персонажами
|
{...}──                             // множество вспомогательных файлов
|
├── APIHelper.swift                // Базовый хелпер для API
└── URLSessionImplementations.swift // Реализации сетевых запросов
```



Модели данных
Artwork - информация о художественном произведении
DimensionsDetail - размеры произведения
DisneyCharacter - данные о персонаже Disney



📋 Требования
iOS 13.0+
Swift 5.7+

🔧 Настройка
Модуль не требует дополнительной настройки. Все API клиенты готовы к использованию сразу после импорта.

👨‍🎓 Otus Homework
Этот проект был разработан в рамках домашнего задания в школе Otus.

Студент: [Дмитрий Никоноров]
Курс: [iOS Professioanl]

📞 Контакты
Если у вас есть вопросы или предложения, создайте issue в репозитории или свяжитесь со мной:

Telegram: @nikonorovdmitriy

Разработано с ❤️ для образовательных целей
